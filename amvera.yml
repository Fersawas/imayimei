meta:
  environment: python
  toolchain:
    name: pip
    version: 3.11
build:
  requirementsPath: requirements.txt
run:
run:
  scriptName: /bin/bash
  args:
    - ./start.sh
  persistenceMount: /data
  containerPort: 80